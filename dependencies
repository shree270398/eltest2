Vagrant.configure("2") do |config|

  config.vm.box = "bento/ubuntu-22.04"

  # Manager Node
  config.vm.define "manager" do |manager|
    manager.vm.hostname = "manager"
    manager.vm.network "private_network", ip: "192.168.56.10"
    manager.vm.provider "virtualbox" do |vb|
      vb.memory = 2048
      vb.cpus = 2
    end
  end

  # Worker 1
  config.vm.define "worker1" do |worker|
    worker.vm.hostname = "worker1"
    worker.vm.network "private_network", ip: "192.168.56.11"
    worker.vm.provider "virtualbox" do |vb|
      vb.memory = 2048
      vb.cpus = 2
    end
  end

  # Worker 2
  config.vm.define "worker2" do |worker|
    worker.vm.hostname = "worker2"
    worker.vm.network "private_network", ip: "192.168.56.12"
    worker.vm.provider "virtualbox" do |vb|
      vb.memory = 2048
      vb.cpus = 2
    end
  end


end



sudo apt install -y docker.io
sudo systemctl enable docker
sudo systemctl start docker
sudo usermod -aG docker $USER
newgrp docker

vagrant up
vagrant ssh master
docker swarm init --advertise-addr 192.168.56.10
docker node ls
9. Deploy your Java application
docker service create \
  --name java-backend \
  --replicas 3 \
  -p 8080:8080 \
  shree270398/java-backend:v1
docker service ls
docker service ps image;


These are the commands we used to allow Jenkins to access Kubernetes (Minikube) during your first pipeline setup.
# 1. Create Jenkins kube directory
sudo mkdir -p /var/lib/jenkins/.kube


# 2. Copy kubeconfig from your user to Jenkins
sudo cp /home/shree/.kube/config /var/lib/jenkins/.kube/config


# 3. Create Minikube directory for Jenkins
sudo mkdir -p /var/lib/jenkins/.minikube


# 4. Copy Minikube certificates
sudo cp -r /home/shree/.minikube/* /var/lib/jenkins/.minikube/


# 5. Give Jenkins ownership
sudo chown -R jenkins:jenkins /var/lib/jenkins/.kube
sudo chown -R jenkins:jenkins /var/lib/jenkins/.minikube


# 6. Edit Jenkins kubeconfig paths
sudo nano /var/lib/jenkins/.kube/config

# 7. Verify Jenkins kubeconfig
sudo -u jenkins kubectl config view


# 8. Verify Jenkins access to Minikube
sudo -u jenkins kubectl get nodes


# 9. Verify pods
sudo -u jenkins kubectl get pods
